import React from 'react'

type Props = {}

//Settings to add: Theme(bright and dark), User Icon, pagination, look up more.

const Settings = (props: Props) => {
  return (
    <div>Settings</div>
  )
}

export default Settings